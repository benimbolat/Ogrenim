import 'package:flutter/material.dart';

class ScaffolLearn extends StatelessWidget {
  const ScaffolLearn({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}